import { EquipmentIconPipe } from './equipment-icon.pipe';

describe('EquipmentIconPipe', () => {
  it('create an instance', () => {
    const pipe = new EquipmentIconPipe();
    expect(pipe).toBeTruthy();
  });
});
